import pandas as pd
import glob
import os


# To merge all the CSV. 

"""files = os.path.join("", "file*.csv")

files = glob.glob(files)

df = pd.concat(map(pd.read_csv, files), ignore_index=True)"""
